server:
  port: ${serverPort:6868}
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: ${defaultZone:http://192.168.17.18:${server.port}/eureka/}

